import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.javier.proleague',
  appName: 'ProLeague',
  webDir: '.output/public'
};

export default config;
